# micro:bit プログラミング教材 - ビデオチュートリアルシナリオ

**バージョン:** 1.0 (Content Plan)  
**形式:** YouTube + 字幕 + 日本語解説  
**対象学習者:** 高校生～大学生（初級～中級）  
**全体時間:** 25分（4 セグメント × 5-7分）

---

## 📺 全体構成と時間配分

| セグメント | タイトル | 所要時間 | 対象トピック |
|-----------|---------|--------|-----------|
| **S1** | GitHub Codespaces で 5分環境セットアップ | 6分 | 環境構築・GitHub Codespaces |
| **S2** | Python で磁力センサーを制御 | 7分 | インペラティブプログラミング・テスト駆動開発 |
| **S3** | TypeScript で型安全に移行する | 7分 | 静的型付け・IDE サポート・コンパイル時エラー検出 |
| **S4** | MakeCode で ブロックエディタ統合 | 5分 | ビジュアルプログラミング・シミュレーター |
| **おまけ** | CI/CD パイプラインの仕組み（GitHub Actions） | 3分 | 自動テスト・品質保証 |

---

## 🎬 Segment 1: GitHub Codespaces 環境セットアップ（6分）

### シーン 1-1: イントロ（0-1分）

**画面**: パワーポイント スライド  
**BGM**: 軽快なテックミュージック（ロイヤリティフリー）

```
【スライド】
タイトル: "micro:bit で 3言語プログラミング"
サブ: "Python → TypeScript → MakeCode"

【ナレーション】
"こんにちは！このビデオでは、
 micro:bit を使って 3つのプログラミング言語を学びます。
 
 磁力センサーの制御を通じて、言語の特性と
 開発環境の違いを実感してみましょう。"
```

### シーン 1-2: GitHub Codespaces の起動（1-4分）

**画面**: ブラウザ画面 + 音声ナレーション

```
【画面】
1. URL を表示: https://codespaces.new/katoy/study-microbit-with-test?quickstart=1
   
   【ナレーション】
   "まずは、GitHub Codespaces を使って
    クラウド上で開発環境を起動します。
    
    このリンクをクリックすれば、
    セットアップは完全に自動化されています。
    ネットワークと GitHub アカウントがあれば、
    わずか 5分で準備完了です。"

2. Codespaces 起動画面を表示（Loading... の進行状況）
   
   【ナレーション】
   "これが Codespaces の起動画面です。
    バックグラウンドで以下が自動実行されています：
    
    ✅ Node.js 22 インストール
    ✅ Python 3.11 セットアップ
    ✅ npm / uv パッケージマネージャ初期化
    ✅ テストの自動実行"
    
3. "Ready to code!" メッセージが表示
   
   【ナレーション】
   "完了です！わずか 5分で、
    すべての環境が準備できました。
    
    プロの開発環境を
    ブラウザから使えるのは、
    学習や協調作業を大きく加速させます。"
```

### シーン 1-3: テスト確認（4-6分）

**画面**: VSCode ターミナル + テスト実行結果

```
【画面】
$ npm run test:all

【テスト結果】
✅ config tests: 30/30 pass
✅ Python tests: 34/34 pass
✅ TypeScript tests: 73/73 pass
✅ MakeCode tests: 2/2 pass

=================== 139 tests passed ===================

【ナレーション】
"環境セットアップの完全さを確認するために、
 すべてのテストを実行しました。
 
 139 個のテストが成功しています。
 これは、環境が 100% 正しく設定されたことを示しています。
 
 では、実装を開始しましょう！"
```

---

## 🎬 Segment 2: Python で磁力センサーを制御（7分）

### シーン 2-1: アルゴリズムの解説（0-2分）

**画面**: ホワイトボードアニメーション / 図解

```
【画面】
X, Y, Z 軸の磁力ベクトルが画面中央に表示
↓ (atan2 計算の視覚化)
角度 (0° - 360°)
↓ (8方向への分類)
"North" / "NE" / "East" / ... / "NW"

【ナレーション】
"磁力センサーの基本は、
 X, Y, Z 軸の磁力を測定することです。
 
 これらの値から、
 atan2 関数を使って角度を計算します。
 
 この角度を 8つの方向に分類することで、
 コンパス（方位磁針）が完成します。"
```

### シーン 2-2: Python コード実装（2-5分）

**画面**: VSCode エディタ + コード実装のタイピング

```
【画面】
compass.py ファイルが開く

【ナレーション - 字幕付き】
"Python で実装します。

 import math を追加して、
 
 get_direction 関数を定義します。
 
 この関数は、x, y の磁力データを受け取り、
 8つの方向のいずれかを返します。"

【実装コード表示】
import math

def get_direction(x, y):
    angle = math.degrees(math.atan2(y, x)) % 360
    
    directions = [
        (0, "N"), (45, "NE"), (90, "E"), (135, "SE"),
        (180, "S"), (225, "SW"), (270, "W"), (315, "NW")
    ]
    
    closest = min(directions, key=lambda d: abs(d[0] - angle))
    return closest[1]

【ナレーション - 継続】
"ここがポイントです。

 1. atan2(y, x) で角度を計算
 2. % 360 で 0-360 度の範囲に正規化
 3. min() で最も近い方向を見つける
 
 これが Python らしい、シンプルで直感的なコードです。"
```

### シーン 2-3: テスト実行と検証（5-7分）

**画面**: ターミナルでテスト実行

```
【画面】
$ cd sample-compass
$ uv run pytest test_compass.py -v

【テスト結果の表示】
test_north_direction PASSED
test_northeast_direction PASSED
test_east_direction PASSED
...
=================== 17 passed in 0.42s =================

【ナレーション】
"すべてのテストが成功しました！
 
 17個のテストケースで、
 8つの方向と、エッジケース（ゼロベクトル）が
 正しく処理されていることを確認しました。
 
 Python の動的型付けは、
 快速なプロトタイピングに最適です。
 
 では、同じロジックを TypeScript で実装してみましょう。"
```

---

## 🎬 Segment 3: TypeScript で型安全に移行（7分）

### シーン 3-1: 型システムの紹介（0-2分）

**画面**: TypeScript vs Python 比較スライド

```
【スライド】
┌─────────────────┬──────────────────────┐
│ Python          │ TypeScript           │
├─────────────────┼──────────────────────┤
│ 動的型付け      │ 静的型付け           │
│ 実行時エラー    │ コンパイル時エラー   │
│ 快速プロト      │ 大規模開発向け       │
│ テスト必須      │ IDE の型チェック支援 │
└─────────────────┴──────────────────────┘

【ナレーション】
"Python と TypeScript の最大の違いは、
 型の扱い方です。
 
 TypeScript は変数の型を事前に宣言することで、
 実行前にエラーを検出できます。
 
 IDE (Integrated Development Environment) が
 型情報を活用して、自動補完や、
 リファクタリング時の影響範囲を正確に提示してくれます。"
```

### シーン 3-2: 型定義とコード実装（2-5分）

**画面**: VSCode エディタ + TypeScript 実装

```
【画面】
compass.ts ファイルが開く

【ナレーション - 字幕付き】
"TypeScript での実装です。

 まず、型定義から始めます。
 Direction は、8つの方向のいずれかに限定した型です。
 MagneticVector は、x, y, z の磁力データを持つオブジェクトです。"

【コード表示】
export type Direction = "N" | "NE" | "E" | "SE" | "S" | "SW" | "W" | "NW";

export interface MagneticVector {
  x: number;
  y: number;
}

【ナレーション - 継続】
"次に、getDirection 関数を実装します。

 引数の型を明示することで、
 間違った型を渡すことはできません。
 
 戻り値も Direction 型に限定されます。"

【コード表示】
export function getDirection(
  vector: MagneticVector,
  fallback: Direction = "N"
): Direction {
  const { x, y } = vector;
  
  if (x === 0 && y === 0) {
    return fallback;
  }
  
  const angle = (Math.atan2(y, x) * 180 / Math.PI) % 360;
  
  const directions: [number, Direction][] = [
    [0, "N"], [45, "NE"], [90, "E"], [135, "SE"],
    [180, "S"], [225, "SW"], [270, "W"], [315, "NW"]
  ];
  
  const closest = directions.reduce((prev, curr) =>
    Math.abs(curr[0] - angle) < Math.abs(prev[0] - angle) ? curr : prev
  );
  
  return closest[1];
}

【ナレーション】
"型定義により、IDE が以下をサポートします：

 ✅ コンパイル時にエラーを検出
 ✅ 関数呼び出しで自動補完
 ✅ リファクタリング時に全参照を追跡
 ✅ ドキュメントなしでもシグネチャから用途が明白"
```

### シーン 3-3: テスト実行とエラーハンドリング体験（5-7分）

**画面**: ターミナル + IDE エラーメッセージ

```
【画面1】
$ cd sample-compass-ts
$ npm test

【テスト結果】
PASS  src/compass.test.ts
  ✓ should detect north direction (2ms)
  ✓ should detect northeast direction (1ms)
  ... (全 48 テスト成功)
  
Test Suites: 1 passed
Tests:       48 passed

【ナレーション】
"48 個のテストが成功しました。
 
 Python と同じロジックですが、
 TypeScript の型システムにより、
 一般的なバグ（型の不一致）は
 実行前に防ぐことができます。"

【画面2】
意図的なバグを示す：
const result: string = getDirection({ x: 10, y: 10 });
                      ↑ コンパイルエラー！
                        Direction 型が必要

【ナレーション】
"例えば、戻り値を string 型に指定すれば、
 IDE はすぐにエラーを表示します。
 
 実行することなく、
 開発中にバグを修正できるのが、
 型安全性の強力な利点です。"
```

---

## 🎬 Segment 4: MakeCode ブロックエディタ統合（5分）

### シーン 4-1: MakeCode Editor へのアクセス（0-1分）

**画面**: ブラウザで makecode.microbit.org を開く

```
【ナレーション】
"最後に、MakeCode のブロックエディタを使った
 実装を見てみましょう。
 
 MakeCode は、ビジュアルプログラミング環境です。
 プログラミング初心者も、
 直感的に micro:bit を制御できます。"
```

### シーン 4-2: ブロックベースの実装（1-4分）

**画面**: MakeCode エディタのブロックをドラッグ＆ドロップ

```
【ナレーション - 字幕付き】
"MakeCode では、ブロックを組み合わせるだけで、
 プログラムが完成します。
 
 では、磁力コンパス機能を実装してみます。"

【ブロック配置のアニメーション】
1. 「on button A pressed」ブロック
2. 「acceleration x」ブロック（磁力センサー）
3. 「atan2 y x」計算ブロック
4. 「show string」表示ブロック

【ナレーション - 継続】
"Python や TypeScript と同じアルゴリズムですが、
 ブロックの組み合わせで実現できます。
 
 - データ型の宣言不要
 - テストコード不要
 - 概念的な理解に集中できる"
```

### シーン 4-3: シミュレーターでの実行確認（4-5分）

**画面**: MakeCode シミュレーター画面

```
【ナレーション】
"MakeCode はシミュレーターを備えており、
 実機なしで動作確認ができます。
 
 右側のシミュレーター画面を見てください。
 micro:bit が表示されています。"

【シミュレーター操作】
1. A ボタンをクリック
2. 画面に「North」表示
3. 別の値を入力して「East」確認
4. 各方向をテスト

【ナレーション】
"このように、
 ビジュアルなアプローチなら、
 プログラミング初心者も安心です。
 
 3 つの言語を通じて、
 プログラミングの本質は変わらず、
 表現方法が異なることが理解できましたね。"
```

---

## 🎬 おまけ: CI/CD パイプライン（3分）

### シーン 5-1: GitHub Actions の役割（0-2分）

**画面**: GitHub Actions ワークフロー図

```
【ダイアグラム】
git push
  ↓
GitHub Actions 自動トリガー
  ↓
┌─────────────┬──────────────┬─────────────┐
│ Python      │ TypeScript   │ MakeCode    │
│ 34 tests    │ 73 tests     │ 2 tests     │
└─────────────┴──────────────┴─────────────┘
  ↓
All 139 tests PASS ✅
  ↓
自動マージ (Green Status)

【ナレーション】
"最後に、自動テストの仕組みを紹介します。

 GitHub Actions は、コードがプッシュされたとき、
 自動的にすべてのテストを実行します。
 
 3 つの言語で 139 個のテストが
 すべて成功することで、
 コードの品質を保証しています。
 
 これが、CI/CD (継続的インテグレーション・デプロイメント) です。"
```

### シーン 5-2: カバレッジと品質メトリクス（2-3分）

**画面**: GitHub Actions ワークフロー実行ログ + カバレッジレポート

```
【ナレーション】
"さらに詳細には、

 ✅ コード品質: 各言語で 100% テストカバレッジ
 ✅ 実行速度: Python 0.42s, TypeScript 1.2s
 ✅ ビルドチェーン: マイクロコントローラ最適化
 
 このように、自動化されたテストにより、
 品質を継続的に保証しています。
 
 大規模なチーム開発でも、
 各メンバーが自信を持ってコードを書けるわけです。"
```

---

## 📹 ビデオ制作チェックリスト

### Pre-Production（制作前準備）

- [ ] スクリプト確定
- [ ] 画面キャプチャ環境セットアップ（OBS、ScreenFlow、QuickTime）
- [ ] BGM・SE（ロイヤリティフリー音源）の決定
- [ ] VSCode カラースキーム・フォントサイズ調整
- [ ] GitHub Codespaces 新規インスタンス準備
- [ ] マイク・オーディオ設定確認

### Production（制作）

#### Segment 1: セットアップ（1日）
- [ ] イントロスライド撮影（1時間）
- [ ] Codespaces 起動のスクリーン撮影（30分）
- [ ] テスト実行結果キャプチャ（15分）
- [ ] ナレーション録音（日本語・英語版）（2時間）
- [ ] テロップ・グラフィックス追加（1時間）

#### Segment 2: Python（1.5日）
- [ ] アルゴリズム解説アニメーション（2時間）
- [ ] VSCode コード実装のタイピング動画（30分）
- [ ] テスト実行デモ（15分）
- [ ] ナレーション録音（1時間）
- [ ] 編集・シンク調整（1時間）

#### Segment 3: TypeScript（1.5日）
- [ ] 型システム比較スライド（1時間）
- [ ] VSCode での実装デモ（30分）
- [ ] IDE エラー表示のキャプチャ（30分）
- [ ] テスト実行＋エラーハンドリング体験（15分）
- [ ] ナレーション・編集（1.5時間）

#### Segment 4: MakeCode（1日）
- [ ] MakeCode エディタのスクリーン撮影（1時間）
- [ ] ブロック配置アニメーション（1.5時間）
- [ ] シミュレーター操作デモ（30分）
- [ ] ナレーション・テロップ（1時間）

#### おまけ: CI/CD（0.5日）
- [ ] GitHub Actions ダイアグラム（1時間）
- [ ] ワークフロー実行ログキャプチャ（30分）

### Post-Production（後処理）

- [ ] 全セグメント統合・トランジション追加（2時間）
- [ ] 字幕作成（日本語・英語）（3時間）
- [ ] 字幕同期・レビュー（2時間）
- [ ] カラーグレーディング・音量調整（2時間）
- [ ] 最終エクスポート（複数フォーマット）（1時間）
- [ ] YouTube アップロード・SEO 最適化（1時間）

### Review & QA
- [ ] 内部レビュー（30分）
- [ ] 字幕精度確認（30分）
- [ ] 音量・音質確認（15分）
- [ ] 複数デバイスでの再生確認（30分）

---

## 📊 期待される効果

| 指標 | 期待値 |
|------|--------|
| ビデオ完成度 | 4.5/5.0 以上（YouTube コミュニティレビュー） |
| 学習効率向上 | ドキュメント比 +40% の理解度 |
| 再現性 | スクリプト + チェックリスト基準で複製可能 |
| 多言語対応 | 日本語・英語字幕デュアルリリース |

---

**作成日**: 2025-01  
**想定制作期間**: 8営業日（4営業日/制作 + 2営業日/編集 + 2営業日/QA）  
**メンテナー**: @katoy  
**ライセンス**: CC0 (パブリックドメイン)
