{
  "name": "study-microbit-with-test",
  "version": "1.0.0",
  "description": "micro:bit 学習プロジェクト",
  "private": true,
  "scripts": {
    "lint:makecode": "cd sample-compass-makecode && npm run lint",
    "lint:ts": "cd sample-compass-ts && npm run build",
    "lint:python": "cd sample-compass && uv run python -m py_compile compass.py conftest.py && echo '✓ Python OK'",
    "lint": "npm run lint:python && npm run lint:ts && npm run lint:makecode",
    "test:makecode": "cd sample-compass-makecode && npm test",
    "test:python": "cd sample-compass && uv run pytest test_compass.py test_build_hex.py -v",
    "integration:python": "cd sample-compass && uv run pytest test_compass_integration.py -v",
    "build:hex:python": "cd sample-compass && uv run python build_hex.py",
    "test:ts": "cd sample-compass-ts && npm run test:unit",
    "integration:ts": "cd sample-compass-ts && npm run test:integration",
    "build:hex:makecode": "cd sample-compass-makecode && npm run build:hex",
    "test:config": "node --test test/*.test.js",
    "test": "npm run test:config && npm run test:python && npm run test:ts && npm run test:makecode",
    "integration": "npm run integration:python && npm run integration:ts",
    "test:all": "npm run test && npm run integration",
    "build:hex": "npm run build:hex:python && npm run build:hex:makecode",
    "audit:npm": "node scripts/audit-npm.js",
    "prepare": "husky install"
  },
  "devDependencies": {
    "husky": "^8.0.3",
    "lint-staged": "^16.4.0"
  },
  "lint-staged": {
    "sample-compass-makecode/**/*.ts": [
      "npm --prefix sample-compass-makecode run lint"
    ]
  },
  "keywords": [
    "microbit",
    "compass",
    "test"
  ],
  "license": "MIT"
}
